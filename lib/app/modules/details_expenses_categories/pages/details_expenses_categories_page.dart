import 'package:flutter/material.dart';

class DetailsExpensesCategoriesPage extends StatelessWidget {
  const DetailsExpensesCategoriesPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
